{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Api",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/backend/api",
            "args": ["-config", "${workspaceFolder}/backend/api/config/config.yaml"],
            "cwd": "${workspaceFolder}",
            "showLog": true
        }
    ]
}
